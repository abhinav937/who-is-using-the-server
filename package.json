{
  "name": "server-monitor-api",
  "version": "1.0.0",
  "description": "API for tracking server user sessions",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^28.0.0"
  }
} 